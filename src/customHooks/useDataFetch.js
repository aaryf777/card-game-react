import React from 'react';
import axios from '../services/httpService';

export default function useDataFetch() {
    const getData = (path,payload) => {}
  return (
    <div>useDataFetch</div>
  )
}
